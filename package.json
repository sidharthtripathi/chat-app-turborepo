{
  "name": "chatapp",
  "packageManager": "npm@10.8.1",
  "scripts": {
    "dev" : "turbo dev",
    "build": "turbo build",
    "prisma:generate" :"turbo prisma:generate",
    "db:init" :"turbo db:init"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.4.2"
  }
}
