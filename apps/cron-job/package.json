{
  "name": "cron-job",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "build": "npx prisma generate && npx prisma db push && npx tsc -b",
    "start": "node build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "redis": "^4.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^5.19.1",
    "ts-node": "^10.9.2"
  }
}
